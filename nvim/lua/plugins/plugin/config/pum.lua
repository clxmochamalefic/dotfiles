---@diagnostic disable: undefined-global

local M = {
  max_width         = 120,
  blend             = vim.g.blend,
  --use_complete  = true,
  border            = 'rounded',
  padding           = true,
  --auto_confirm_time = 3000,
  --auto_select       = false,
}

return M
