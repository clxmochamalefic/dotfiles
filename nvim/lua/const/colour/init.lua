-- ---------------------------------------------------------------------------
-- ONEPOINT COLOURS
-- ---------------------------------------------------------------------------

local M = {
  vivid = require("const.colour.vivid"),
  azure = require("const.colour.azure"),
  transparent = "none",
  blend = {
    value = 20,
    max = 100,
  },
}

return M

